# =========================
# Compose-wide (non-secret)
# =========================
# Optional: sets the project prefix used in container/volume/network names.
# COMPOSE_PROJECT_NAME=real-time-streaming

# =========================
# MinIO (S3-compatible)
# =========================
# Root access key (equivalent to AWS_ACCESS_KEY_ID). Change for security.
MINIO_ROOT_USER=minio
# Root secret key (equivalent to AWS_SECRET_ACCESS_KEY). Use a strong value.
MINIO_ROOT_PASSWORD=minio12345678

# (Optional) TLS/KMS for at-rest encryption in dev; leave commented if unused.
# MINIO_KMS_SECRET_KEY=mykey:BASE64_32BYTE_KEY

# =========================
# Kafka (dual listeners)
# =========================
# Host-facing port for OUTSIDE listener; INSIDE remains 9092 for containers.
KAFKA_OUTSIDE_PORT=9094
# Broker ID for single-broker setup (keep 1 unless you add brokers).
KAFKA_BROKER_ID=1

# =========================
# MariaDB (Hive Metastore DB)
# =========================
# Root password for the MariaDB server (used by init scripts and maintenance).
MARIADB_ROOT_PASSWORD=your_root_password
# Metastore DB name.
MARIADB_DATABASE=hive_metastore
# Metastore DB user Hive will use.
MARIADB_USER=hive
# Password Hive will use to connect to the metastore.
MARIADB_PASSWORD=hive_password

# =========================
# MongoDB (optional in your stack)
# =========================
# Admin user for Mongo; created on first init only.
MONGO_INITDB_ROOT_USERNAME=mongouser
MONGO_INITDB_ROOT_PASSWORD=mongopassword

# =========================
# Spark (container runtime)
# =========================
# Spark HOME for caching Ivy/Maven artifacts inside the container.
HOME=/tmp/spark-home
# NOTE: Do NOT redefine MINIO creds here in `.env` using ${...:-...} syntax.
# That syntax is not expanded inside .env files (see notes below).

# =========================
# Marquez (Postgres database)
# =========================
# DB settings used by the Postgres container for Marquez.
POSTGRES_DB=marquez
POSTGRES_USER=marquez
POSTGRES_PASSWORD=marquez

# =========================
# Marquez (API service)
# =========================
# JDBC URL the Marquez API uses to reach Postgres (inside the compose network).
MARQUEZ_DB_URL=jdbc:postgresql://marquez-db:5432/marquez
# DB user/password that the Marquez API will use.
MARQUEZ_DB_USER=marquez
MARQUEZ_DB_PASSWORD=marquez
# Marquez API port (container listens on 5000; you map it in compose).
MARQUEZ_PORT=5000
